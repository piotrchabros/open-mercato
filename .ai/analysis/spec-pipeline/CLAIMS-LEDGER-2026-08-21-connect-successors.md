# Claims ledger — Connect successor and upstream specifications

Specifications: the four `2026-08-21-connect-{foundation-ingest,inbox-operations,customer-projection,operational-metrics}.md` successors and five upstream prerequisites: `2026-08-21-connect-upstream-{send-contract,thread-reader,customer-interaction-contract,inbound-envelope,shared-channel-authorization}.md`.

Verified by: Codex `/root/claims_verifier`
Author: previous/root spec-writing agent
UNGATED: no

Fresh verification after the 2026-08-21 corrections. Falsifiers were recorded before the cited paths were reopened. Proposed Connect and PR A/B/C behavior is a requirement, not a claim that it ships today.

| # | Claim | Where | Performs it | Falsifier | Verdict | Notes |
|---|---|---|---|---|---|---|
| 1 | Existing `communication_channels` inbound events include `communication_channels.message.received`. | Foundation `:8`, `:23` | `packages/core/src/modules/communication_channels/events.ts:3-11` registers it; `packages/core/src/modules/communication_channels/commands/ingest-inbound-message.ts:548-563` emits it persistently. | No registered event, or ingest never emits it. | **CONFIRMED** | Registration and writer both exist. |
| 2 | The event's `conversationId` is the hub-owned `ExternalConversation.id`, and the hub persists the same value in `ChannelThreadMapping.externalConversationId`. | Foundation `:23`, `:64`, `:104` | The writer emits `conversationId: conversation.id` at `packages/core/src/modules/communication_channels/commands/ingest-inbound-message.ts:548-561` and writes `externalConversationId: conversation.id` into the mapping at `:430-438`. The mapping field is the UUID logical link at `packages/core/src/modules/communication_channels/data/entities.ts:330-347`. | The emitted field is absent/different, or the mapping is keyed from the provider reference instead. | **CONFIRMED** | The earlier event-field error is fully corrected; provider reference remains `externalThreadRef`. |
| 3 | `communication_channels` stores transport conversations/messages. | Foundation `:19` | Ingest composes the platform message at `packages/core/src/modules/communication_channels/commands/ingest-inbound-message.ts:381-425` and writes its mapping at `:430-440`; the external conversation is persisted by the same command against `packages/core/src/modules/communication_channels/data/entities.ts:187-202`. | Ingest emits only transient data and persists no conversation/message/thread state. | **CONFIRMED** | The revised spec no longer makes an unbounded absence claim about every service-work capability. |
| 4 | The reproduced Connect query-index route failed OpenAPI bundling because `#generated/entities/connect_case` could not resolve; this does not imply every non-core indexer fails. | Foundation `:28` | The executed imports, warning, removal test, and successful rerun are preserved at `.ai/analysis/spec-pipeline/SPIKE-FINDINGS-2026-08-21-connect.md:181-219`. The resolver hardwires `#generated/*` to `coreGeneratedRoot` at `packages/cli/src/lib/generators/openapi.ts:320-330,476-483`. | The route did not use that import, the resolver selects the owning package, or removal did not clear the error. | **CONFIRMED** | The former categorical claim is narrowed to the reproduced configuration and performing resolver. |
| 5 | A non-core ORM-backed generic CRUD route without query-index fields is a shipped precedent. | Foundation `:28` | `packages/webhooks/src/modules/webhooks/api/webhooks/route.ts:71-80` instantiates `makeCrudRoute` with ORM/auth/events/list schema and no `indexer`, `entityId`, or generated fields. | Every non-core ORM CRUD route requires query-index configuration. | **CONFIRMED** | Directly reusable list precedent. |
| 6 | System-authored inbound messages can fall outside the existing sender-or-recipient participant predicate. | Inbox `:17`; Thread Reader `:5`, `:30` | Ingest supplies a bot/fallback/sentinel sender and may supply no recipients at `packages/core/src/modules/communication_channels/commands/ingest-inbound-message.ts:381-415`; fallback is performed at `packages/core/src/modules/communication_channels/lib/system-user.ts:39-73`. Participant scope admits only matching sender or non-deleted recipient at `packages/core/src/modules/messages/lib/participantScope.ts:47-63`. | Every viewer is always sender/recipient, or the predicate has a tenant nonparticipant branch. | **CONFIRMED** | Confirms the security boundary PR C must explicitly relax. |
| 7 | The hub has an existing DI entry/pattern for source-owned cross-module facades. | Inbox `:54`; Thread Reader `:13`, `:39` | `packages/core/src/modules/communication_channels/di.ts:15-39` registers `communicationChannelsSendAsUser`; `packages/core/src/modules/communication_channels/lib/send-as-user.ts:63-77` implements it. | The hub registers only entity classes, or callers must use HTTP self-calls. | **CONFIRMED** | The proposed reader itself does not yet exist. |
| 8 | The existing send-as-user path rejects actors who do not own the channel. | Send Contract `:9` | `packages/core/src/modules/communication_channels/lib/send-as-user.ts:88-103` returns 403 unless `channel.userId === actor.userId`. | A non-owner or shared null-user channel passes the facade. | **CONFIRMED** | PR A correctly books shared sending as new authorization work. |
| 9 | The current send facade already enqueues outbound delivery. | Inbox architecture; Send Contract `:51` | `packages/core/src/modules/communication_channels/lib/send-as-user.ts:269-275` constructs a payload and calls `queue.enqueue`. | The facade calls the provider synchronously and writes no job. | **CONFIRMED** | Connect's durable attempt/reconciliation state remains new work. |
| 10 | The hub has a source-owned management authorization path whose shared-channel branch requires an elevated feature. | Send Contract `:13` | `packages/core/src/modules/communication_channels/lib/access-control.ts:117-142` checks personal ownership and calls `authorizeFeatures` for `userId == null`. | Shared management succeeds without an elevated feature, or no helper performs it. | **CONFIRMED** | Supports delegation instead of a caller bypass flag. |
| 11 | Relaxing `messages.Message.senderUserId` is unnecessary for system-authored inbound sends. | Send Contract `:9`, `:13`, `:55` | Ingest resolves a bot/fallback/sentinel UUID at `packages/core/src/modules/communication_channels/commands/ingest-inbound-message.ts:410-414`; compose writes it at `packages/core/src/modules/messages/commands/messages.ts:305-329`; the entity stays required at `packages/core/src/modules/messages/data/entities.ts:53-54`. | Existing inbound compose must insert null or cannot write without a column change. | **CONFIRMED** | The upstream spec correctly drops the former column change. |
| 12 | Current `customers.interactions.create` accepts caller-supplied IDs. | Projection `:20`, `:85`; Customer Contract `:5`, `:9` | `packages/core/src/modules/customers/data/validators.ts:463-468` accepts optional `id`; `packages/core/src/modules/customers/commands/interactions.ts:343-375` maps it and `:377-423` persists it. | The validator strips/rejects `id`, or the writer generates another ID. | **CONFIRMED** | Confirms deterministic identity only. |
| 13 | Current create flushes duplicate deterministic IDs without scoped duplicate-key recovery. | Projection `:20`, `:38`; Customer Contract `:5`, `:38` | `packages/core/src/modules/customers/commands/interactions.ts:393-423` builds, persists, and flushes with no unique-violation catch/lookup branch. | A 23505 branch validates the existing tenant/customer winner and returns success. | **CONFIRMED** | The former false “23505 comes free” premise is now a correct gap; PR B owns new recovery. |
| 14 | Connect can call the existing interaction service cross-module without importing the customer entity. | Customer Contract `:9`; Projection `:20`, `:50` | `apps/mercato/src/modules/example_customers_sync/lib/sync.ts:859` executes `customers.interactions.create` through the command bus; source handler is `packages/core/src/modules/customers/commands/interactions.ts:377-423`. | No cross-module caller reaches it without direct ORM access. | **CONFIRMED** | Tombstone remains proposed work. |
| 15 | `communication_channels` owns the link direction/transport metadata needed by PR C. | Thread Reader `:9`, `:13` | Ingest writes the channel link at `packages/core/src/modules/communication_channels/commands/ingest-inbound-message.ts:475-512`; `MessageChannelLink` stores direction, delivery status, payload/content type/metadata at `packages/core/src/modules/communication_channels/data/entities.ts:285-324`. | Those values are owned entirely by `messages`, or the hub has no link writer. | **CONFIRMED** | Supports locating the reader in the hub. |
| 16 | Shared table, guarded-mutation, conflict, KPI, and chart components exist for reuse. | Inbox `:95`, `:150`; Metrics `:79`, `:117` | `packages/ui/src/backend/DataTable.tsx:1207-1224`; `packages/ui/src/backend/injection/useGuardedMutation.ts:33-65`; `packages/ui/src/backend/conflicts/index.ts:82-100`; `packages/ui/src/backend/charts/index.ts:1-7`; `packages/ui/src/backend/charts/KpiCard.tsx:91-114`. | Any named mechanism is absent or does not perform its function. | **CONFIRMED** | Confirms components, not Connect screens/datasets. |
| 17 | Nullable-organization records on ordinary ORM CRUD are partitioned away from organization-scoped actors, making nullable shared-Inbox Cases unusable through that path. | Foundation `:19`, `:27` | `extractScopeFromAuth` maps a missing org to `null`, and `buildScopedWhere` writes that exact org predicate at `packages/shared/src/lib/api/crud.ts:5-38`. The executed two-row SQL reproduction and CRUD consequence are preserved at `.ai/analysis/spec-pipeline/SPIKE-FINDINGS-2026-08-21-connect.md:103-123`. | An organization-scoped actor can read a null-org row through ordinary scoped CRUD, or an org-less actor's predicate includes organization rows. | **CONFIRMED** | Supports the owner decision to require one organization per shared channel/Case; it does not claim the platform already enforces that new requirement. |
| 18 | The current `message.received` event is identifier/scope-only and carries no sender handle, headers, subject, or body. | Foundation `:23`, `:67`; Inbound Envelope `:5`, `:9`, `:29` | The sole ingest emission at `packages/core/src/modules/communication_channels/commands/ingest-inbound-message.ts:548-563` publishes message/external-message/link/conversation/channel/provider/type/direction/tenant/organization fields only. The source metadata is instead persisted on `MessageChannelLink` at `:482-496`. | Any emitted payload property contains sender, headers, subject, body, or raw channel payload. | **CONFIRMED** | Confirms both the missing ingest inputs and that the current persistent event contains no raw PII. Contract D is correctly proposed as new source-owned work. |
| 19 | The existing hub delivery command is the provider-send owner, and its current crash window is explicitly at-least-once rather than safely reconcilable. | Inbox `:21`; Send Contract `:13`, `:23-24` | `packages/core/src/modules/communication_channels/commands/deliver-outbound-message.ts:401-420` documents the post-provider/pre-flush duplicate window and performs the only production `adapter.sendMessage` call; `:426-490` persists success and emits the existing outcome afterward. | Another production module calls `adapter.sendMessage`, or the current command has a provider lookup/idempotent recovery path that prevents resend after that crash window. | **CONFIRMED** | PR A correctly keeps provider ownership in the hub while booking correlated outcome/lookup/no-resend semantics as new work. |
| 20 | A shared, organization-scoped `ProgressJob` service exists with create/start/update/terminal/cancel behavior suitable for metrics rebuild progress. | Metrics `:85`, `:87` | `packages/core/src/modules/progress/data/entities.ts:4-75` stores status/progress/cancellation and scope; `packages/core/src/modules/progress/lib/progressServiceImpl.ts:256-295` creates and starts scoped jobs; `:506-555` performs cancellation/request semantics. | No shared job can be created in tenant+organization scope, expose progress states, or accept cancellation. | **CONFIRMED** | Confirms reuse of the progress primitive, not the proposed overlap/partition/atomic aggregate policy. |
| 21 | Existing declared Customer detail spots and DataTable injection hosts are available for Projection widgets, so the Projection spec does not require a new host address. | Projection `:49`, `:140` | `packages/core/src/modules/customers/extension-points.ts:16-38` declares people/company table and detail header/status/footer hosts; the current person page mounts the declared detail hosts at `packages/core/src/modules/customers/backend/customers/people-v2/[id]/page.tsx:526-527,743`, and the company page mounts them at `packages/core/src/modules/customers/backend/customers/companies-v2/[id]/page.tsx:488-489,623`. | No declared host exists on the target Customer details/tables, or the declarations have no reachable mount. | **CONFIRMED** | Confirms available host addresses and call sites. Projection still owes the data supplier and renderer it explicitly plans. |
| 22 | Existing people/company list APIs provide authenticated, feature-gated, tenant-and-organization-scoped candidate discovery with search and page size capped at 100. | Projection `:83` | People defines search/page bounds and view metadata at `packages/core/src/modules/customers/api/people/route.ts:44-76`, then configures tenant/org ORM scope at `:80-93`. Companies performs the same at `packages/core/src/modules/customers/api/companies/route.ts:48-81,85-100`. | Either GET is unauthenticated/unfeature-gated, lacks search/bounded paging, or omits tenant/organization scope. | **CONFIRMED** | Confirms reuse of the authorized server APIs; masking and final selected-UUID revalidation remain Projection work. |
| 23 | Current channel authorization has strict owner-only personal channels and tenant-wide null-user channels, but no organization-scoped shared-inbox membership decision on those paths. | Shared Authorization `:9`, `:13`; Send `:11`, `:15`; Thread Reader `:15` | `assertCanAccessChannel` returns for every `userId == null` channel and otherwise enforces exact owner at `packages/core/src/modules/communication_channels/lib/access-control.ts:62-98`; management similarly uses owner-or-elevated-feature, not membership, at `:100-142`. Current tenant-scoped provisioning deliberately persists shared channels with `organizationId: null` at `packages/core/src/modules/communication_channels/commands/connect-credential-channel.ts:220-234`. | Current shared-channel access validates an organization-scoped channel-user membership record, or current shared provisioning always writes one non-null owning organization. | **CONFIRMED** | This is the load-bearing gap Contract E now owns. The proposed membership entity/features/non-null owner are not presented as current behavior. |
| 24 | Files under a module's `subscribers/` directory are auto-discovered and emitted into the module registry, so a Connect subscriber is registered independently of whether its optional peer capability is currently usable. | Foundation `:67-69` | The scanner declares `subscribers` as a script-file discovery folder at `packages/cli/src/lib/generators/scanner.ts:150-153`; `discoverSubscribers` scans/resolves every matching module file at `packages/cli/src/lib/generators/module-registry.ts:1862-1885`; registry generation includes the discovered array at `:3734-3735,3763-3772`. | Subscriber files require runtime conditional registration outside discovery, or unavailable peers prevent the subscriber from being emitted into the registry. | **CONFIRMED** | The remediation correctly moves optional-prerequisite handling inside an always-registered handler. The proposed versioned activation service and inert/error behavior are new Connect work. |
| 25 | Existing organization administration provides the initial administrator authority and guarded organization-management UI/API that Contract E can use for first-admin bootstrap. | Shared Authorization `:28` | Directory setup grants the default `admin` role `directory.organizations.view/manage` at `packages/core/src/modules/directory/setup.ts:40-48`; the organizations API requires those features at `packages/core/src/modules/directory/api/organizations/route.ts:88-104`; the existing administration page checks the manage feature before exposing management behavior at `packages/core/src/modules/directory/backend/directory/organizations/page.tsx:70-95`. | Default administrators receive no organization-management grant, or no guarded organization administration surface exists. | **CONFIRMED** | Confirms a non-circular bootstrap authority. Contract E still owns shared-channel `.manage`, membership commands, self-escalation denial, and last-manager protection. |

CONFIRMED: 25    OVERSTATED: 0    REFUTED: 0    UNCITABLE: 0

## Corrected-claim disposition

| Prior finding | Revised disposition |
|---|---|
| Event incorrectly asserted `externalConversationId` | Corrected and **CONFIRMED** as `event.conversationId = ExternalConversation.id`, persisted in Connect's external-conversation field; row 2. |
| “Every non-core query index breaks OpenAPI” | Withdrawn. The exact Connect reproduction and core-only resolver mechanism are **CONFIRMED**; row 4. |
| Existing customer create treated as idempotent | Corrected. Current no-recovery behavior is **CONFIRMED**, while scoped duplicate reconciliation is explicitly new PR B work; rows 12-13. |
| Blanket “existing encryption rules” assertion | Withdrawn as a platform claim. Projection `:74` now enumerates future storage decisions: encrypt link/unlink `reason` with the Connect hook; store customer references only as opaque kind/UUID pairs; exclude handles, reasons, and wrap-up text from search. These are implementation requirements/tests, not current-code claims. |
| Nullable organization treated as tenant-visible metadata | Replaced by the owner decision that shared channels and Cases have one non-null organization. The ordinary CRUD visibility consequence supporting that decision is **CONFIRMED**; row 17. The non-null channel/Case enforcement itself is correctly proposed work. |
| Foundation assumed event metadata sufficient for identity/classification | Withdrawn. The event's identifier-only payload and source-owned persisted metadata are **CONFIRMED**; row 18. Contract D now explicitly owns the bounded authorized envelope. |
| Connect was described as owning/provider-calling its own send worker | Corrected to durable Connect dispatch into the existing hub provider owner. Current hub ownership and its at-least-once crash gap are **CONFIRMED**; row 19. PR A owns the new outcome and read-only reconciliation evidence. |
| Send/thread/envelope each carried overlapping shared-channel authorization work | Extracted into Contract E. The current owner-or-tenant-wide model and absence of membership checks on those performing paths are **CONFIRMED**; row 23. Contract E's organization owner, membership record, and read/send features remain proposed work. |
| Optional peer absence was described as conditional subscriber registration | Corrected to the platform's actual auto-discovery model. Always-emitted subscriber registration is **CONFIRMED**; row 24. Capability/version gating is now explicitly handler-owned proposed work. |
| Subject/wrap-up handling relied on unstated storage policy | Withdrawn as a platform claim. Foundation `:83` and Inbox `:90,166` now explicitly require encrypted subject/wrap-up/send payload, safe minimized labels/fingerprints, search/log/analytics exclusion, retention erasure, and leakage tests. These are implementation requirements, not claims that current Connect code already performs them. |
| Contract E lacked a non-circular first manager | Corrected to bootstrap through existing organization administration. The default admin grant and guarded administration surfaces are **CONFIRMED**; row 25. Contract E still owns its narrower membership-management controls. |

## Load-bearing failures

None. All current-platform premises carrying reuse, boundary, or upstream-scope decisions have performing citations and survived their falsifiers. Proposed Contracts A/B/C/D/E remain unimplemented by definition and need implementation verification before downstream release.

## Anti-evidence patterns hit

None in the revised documents. The prior patterns were removed: the consumer uses the emitted event field; caller-supplied ID support is no longer conflated with duplicate recovery; the spike claim is scoped to its exact route/import; and encryption is expressed as enumerated future decisions.
